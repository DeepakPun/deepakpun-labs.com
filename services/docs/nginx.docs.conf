events { worker_connections 1024; }
http {
    include /etc/nginx/mime.types;
    server {
        listen 80;
        location / {
            root /usr/share/nginx/html;
            index index.html index.htm;
            # Crucial: redirects 404s back to React's engine
            try_files $uri $uri/ /index.html; 
        }
    }
}
